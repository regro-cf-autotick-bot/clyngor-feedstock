{% set name = "clyngor" %}
{% set version = "0.3.32" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 4a1a25408122dcc3494b8d927dc969086b6a8bef6e2dc8352ed535ba54d5ecf8

build:
  number: 0
  noarch: python
  script: '{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv '

requirements:
  host:
    - pip
    - python
    - arpeggio >=1.6.1
    - pypeg2 >=2.15.2
    - pytest >=3.2.1
  run:
    - python
    - arpeggio >=1.6.1
    - pypeg2 >=2.15.2

test:
  imports:
    - clyngor

about:
  home: https://github.com/aluriak/clyngor
  license: GPL-3.0-or-later
  license_family: GPL
  license_file: LICENCE
  summary: Handy python wrapper around Potassco's Clingo ASP solver.

extra:
  recipe-maintainers:
    - raivivek
